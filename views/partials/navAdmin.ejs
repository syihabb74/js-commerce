<nav class="bg-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between">
            <div class="flex space-x-7">
            </div>
            <div class="hidden md:flex items-center space-x-1">
                <a href="/admin" class="py-4 px-2 font-semibold transition duration-300 <%= isActive(activeNav, 'home') %>">Home</a>
                <a href="/admin/products"
                    class="py-4 px-2 font-semibold transition duration-300 <%= isActive(activeNav, 'products') %>">Product</a>
                <a href="/admin/logout"
                    class="py-4 px-2 font-semibold transition duration-300">Logout</a>
            </div>
            <div class="md:hidden flex items-center">
                <button class="outline-none mobile-menu-button">
                    <svg class="w-6 h-6 text-gray-500 hover:text-green-500" fill="none" stroke-linecap="round"
                        stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                        <path d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</nav>